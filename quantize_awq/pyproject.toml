[project]
name = "quantize-awq"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "llmcompressor>=0.9.0",
    "datasets",
    "transformers",
    "torch",
    "accelerate",
]

[tool.uv]
dev-dependencies = []
